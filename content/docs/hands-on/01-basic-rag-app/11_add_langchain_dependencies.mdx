---
title: 11. Add LangChain Dependencies
description: LangChain の依存関係を追加する
icon: Rocket
---

`langchain` と `@langchain/openai` パッケージを追加する。

```bash title="Terminal"
bun add langchain @langchain/openai
```

コミットを行う。

```shell title="Terminal"
git add .
git commit -m '11. Add LangChain Dependencies'
git push
```
